import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ClickFunnels Clone - Build High-Converting Sales Funnels',
  description: 'Create, manage, and optimize sales funnels with our powerful drag-and-drop builder',
  keywords: 'sales funnels, marketing, conversion optimization, landing pages',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}

